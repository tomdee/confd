[template]
src = "bird_aggr.cfg.template"
dest = "/config/bird_aggr.cfg"
prefix = "/calico/ipam/v2/host/HOSTNAME/ipv4/block"
keys = [
    "/",
]
reload_cmd = "echo -e 'POST /containers/calico-bird/kill?signal=HUP HTTP/1.0\r\n' | nc -U /docker.sock"
